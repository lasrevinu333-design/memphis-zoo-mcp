name: Night integration audit backend source snapshot

on:
  push:
    branches:
      - internal/night-audit-source-20260715-v2
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Export current backend source
        run: |
          git archive --format=tar.gz --output=/tmp/backend-night-audit-current.tar.gz HEAD
          sha256sum /tmp/backend-night-audit-current.tar.gz > /tmp/backend-night-audit-current.tar.gz.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: backend-night-audit-current
          path: |
            /tmp/backend-night-audit-current.tar.gz
            /tmp/backend-night-audit-current.tar.gz.sha256
          if-no-files-found: error
          retention-days: 1
