name: Night audit backend source snapshot

on:
  push:
    branches:
      - internal/night-audit-source-20260715
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Export exact source
        run: |
          set -euo pipefail
          git archive --format=tar.gz --output=/tmp/backend-night-audit.tar.gz HEAD
          sha256sum /tmp/backend-night-audit.tar.gz > /tmp/backend-night-audit.tar.gz.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: backend-night-audit-source
          path: |
            /tmp/backend-night-audit.tar.gz
            /tmp/backend-night-audit.tar.gz.sha256
          if-no-files-found: error
          retention-days: 1
