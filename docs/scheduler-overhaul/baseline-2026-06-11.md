# Memphis Scheduler Overhaul Baseline — 2026-06-11

Generated by Omega on 2026-06-11T18:43:30-05:00 for the scheduler complete rebuild branch.

## Scope

This is the Phase 0 / Phase 1 safety baseline for the Memphis Zoo custodial scheduler rebuild.

The rebuild stays **preview-first**:

- Build additive SCH2 preview/audit objects before touching staff-facing compatibility tables.
- Generate candidate schedule runs into preview/audit tables first.
- Publish only after read-only gates prove hard constraints are clean.
- Keep the current legacy generator as rollback until ops validates the new output.

## Current branch

- Repo: `/home/eric/Projects/memphis-zoo/memphis-zoo-mcp`
- Branch: `scheduler-complete-overhaul-2026-06-11`
- Starting commit observed before artifact work: `24c6112 fix(messaging): suppress off-shift phone alerts`

## No production scheduler writes in this phase

No production scheduler writes are part of this artifact step.

Specifically not allowed in Phase 0 / Phase 1 safety setup:

- No publish into `public.daily_schedule_assignments`.
- No legacy generator cutover.
- No feature flag flip from `legacy`.
- No broad manual repair SQL.
- No staff-facing UI behavior change.

The only acceptable live checks before the next phase are read-only SQL probes through the approved read-only path.

## Read-only DB gate

Before any production write or publish attempt, run these read-only SQL gates:

- `scripts/sql/check-scheduler-open-owner-contract.sql`
- `scripts/sql/check-scheduler-exception-contract.sql`
- `scripts/sql/check-my-schedule-source-contract.sql`
- `scripts/sql/missing-assignments-report.sql`
- `scripts/sql/check-sch2-hard-constraints.sql`
- `scripts/sql/check-sch2-publish-compatibility.sql`
- `scripts/sql/check-sch2-workload-fairness.sql`
- `scripts/sql/check-sch2-route-span.sql`
- `scripts/sql/check-sch2-lunch-coverage.sql`
- `scripts/sql/check-sch2-restroom-rebalance.sql`

Gate rule:

- Existing scheduler gates should return zero hard violations unless a known current legacy issue is being deliberately documented.
- SCH2 gates may initially return missing-object readiness rows because SCH2 preview objects are not installed yet.
- Once SCH2 preview objects exist, hard violation rows must be zero before publish.

## Baseline live read-only observations

Observed through live read-only Supabase checks during branch setup:

- `check-scheduler-open-owner-contract` equivalent currently returns **6 rows**.
  - All observed rows were Herpetarium `OPEN` / missing owner rows opened by the Kathy east-boundary guard.
  - This is a current legacy-generator weakness, not a permitted SCH2 publish state.
- `missing-assignments-report` equivalent returned **0 rows** for the visible window.
- `check-my-schedule-source-contract` equivalent returned **0 rows** for the current service date.

## Scheduler rebuild guardrails to preserve

- Primate Canyon and Cat Country stay response-only / no-clean.
- Monday gift shops remain 8:00 AM reminders only, not scan-cleaning work after 9:45 or lunch coverage.
- Herpetarium must not appear on Wednesdays.
- Lunch coverage must not assign same-lunch or overlapping-lunch employees to cover each other.
- Michael keeps special afternoon-call visibility without becoming a normal 9:45/restroom-balanced worker.
- Kathy stays west of the east-boundary preference unless explicitly and safely overridden by policy.
- Bonobos Restrooms and Splash Pad Restrooms remain one operational bundle with Event Center when Splash Pad is open.
- Employee-facing pages stay simple and actionable; no status-chip clutter.

## Rollback posture

Rollback stays legacy-first until SCH2 is proven:

1. Leave legacy generator active.
2. Treat SCH2 preview/audit tables as additive, not the source of staff-facing truth.
3. If a later SCH2 publish happens, rollback must use audited previous rows and exact expected row counts.
4. Never use broad unscoped schedule-row repair for rollback.
5. Preserve audit evidence even after rollback.

## Next safe build steps

1. Keep `npm run test:scheduler-overhaul` passing for static artifact safety.
2. Add additive SCH2 preview tables with an inverse rollback migration plan.
3. Add `sch2_build_work_items` as preview-only.
4. Generate preview for a fixture date without publishing.
5. Compare current legacy output against preview before any staff-facing cutover.
