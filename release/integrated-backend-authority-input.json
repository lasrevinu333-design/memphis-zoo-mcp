{
  "release_contract_version": "offline-authority.v5",
  "accepted_engine_contract": {
    "scan": "scan.v2",
    "queue": "indexeddb-v4",
    "completion": "tool_complete_session via authenticated compatibility adapter"
  },
  "required_engine_contract": {
    "scan": "scan.v4.snapshot-bound-authority",
    "queue": "indexeddb-v6-offline-authority",
    "activation": "tool_start_offline_occurrence",
    "completion": "tool_commit_cleaning_workflow with context_id and submission_proof"
  },
  "backend_contract": {
    "scan": "scan.v4.snapshot-bound-authority",
    "authority": "offline-authority.v5",
    "execution_boundary": "CUSTODIAL_BACKEND_PROOF_SECRET",
    "bridge_backend_source": "src/scan-authority-cutover.js:runCanonicalScanRpc",
    "health_gate": "GET /scan-api/authority-health reads custodial_backend_authority_health plus the durable receipt produced only after the designated phone traverses the authenticated native-vault /scan-api/rpc route; canary resume binds that exact receipt and requires every active employee-device credential to be confirmed and match the deployed DEVICE_CREDENTIAL_SECRET generation",
    "device_credential_secret_gate": "Every active employee-device credential must be confirmed and carry the exact deployed device-credential-hmac.v1 secret-generation identifier before canary resume. A changed generation requires manager-code phone recovery that preserves phone-local work; legacy secret fallback is forbidden."
  },
  "cutover": {
    "source_identity": {
      "kind": "external_signed_release_attestation",
      "acceptance_input_contract": "The cutover invocation must supply a regular, mode-0444, Ed25519-signed release attestation outside this worktree. It binds the semantic release, backend commit/tree/evidence digest/evidence blob, frontend commit, and schema fingerprint. The executable gate deterministically enumerates every tracked path and Git mode from that exact tree, compares every worktree path directly with its exact tree blob and mode, and rejects hidden index flags or Git-environment substitution before any database access. Generated evidence is never its own identity authority.",
      "generated_evidence_path": "release/integrated-backend-authority-evidence.json",
      "generated_evidence_excluded_from_content_identity": true,
      "authority_inventory": {
        "source": "all-tracked-paths-in-external-expected-tree",
        "order": "git ls-tree -r -z bytewise path order",
        "include": "every regular tracked path in the caller-supplied expected tree",
        "exclude": [
          "release/integrated-backend-authority-evidence.json"
        ],
        "reason": "The generated evidence is separately compared with its exact expected-tree blob and is excluded only to prevent self-reference. All migrations, release inputs, schema-fingerprint inputs, rebuild and database suites, provenance and suite-order gates, final-operational and named-manager gates, generator, checker, package commands, and every other tracked source path are included without a hand-maintained allowlist."
      }
    },
    "phase_order": [
      "prepare distinct CUSTODIAL_BACKEND_PROOF_SECRET, CUSTODIAL_NATIVE_ROUTE_PROOF_SECRET, and DEVICE_CREDENTIAL_SECRET values (minimum 32 characters each); preserve all phone-local work and require manager-code recovery for any active credential from a different secret generation without exposing any secret",
      "capture the production catalog through the read-only Supabase boundary and require it to equal the observed clean 113-migration fingerprint recorded in release/production-migration-state.json",
      "verify production project, observed migration head, catalog/privilege fingerprint, backup receipt, and exact source attestation against release/production-migration-state.json",
      "after explicit production-change admission, apply only the one exact pending credential-replacement-lineage migration, then require the clean 114-migration target fingerprint and zero remaining pending migrations without replaying the four already-applied release migrations",
      "deploy the canonical-only backend only after all authoritative procedures above are present and verified; missing canonical writers fail closed",
      "require a green authority health gate and direct-DML denial probes before routing traffic"
    ],
    "production_migration_state": "release/production-migration-state.json",
    "rollback": {
      "precondition": "retain the exact bridge backend artifact, both release-proof secrets, and the exact employee-device credential secret generation until all authority health and restoration probes are green",
      "action": "pause the exact physical canary through the durable control; if canonical authority is missing or broken, restore the captured known-good function set forward, verify health, then explicitly resume",
      "restoration_checks": [
        "custodial_backend_authority_health returns ok=true",
        "the designated phone's authenticated native-vault /scan-api/rpc request records an immutable exact-device, credential, backend-commit, and release receipt before canary resume",
        "exact start and completion replay return one durable proof, reconciliation, and completion",
        "offline snapshot activation requires exact issued identity; frozen exact replay remains attributable to the original actor after reassignment",
        "scan evidence remains exact-shape and provenance-labeled after replay",
        "service_role direct insert/update/delete/truncate probes are denied",
        "named-manager reconciliation read, exactly-once disposition replay, and notification retry state remain available"
        ,"present-but-broken and missing canonical authority functions restore to their captured known-good definitions while the canary remains paused"
        ,"schedule, turnover, occurrence, and dashboard service dates all change at the same 04:00 America/Chicago boundary"
        ,"employee and manager notifications use the same service date; activation replay returns frozen actor identity and its rollback definition remains exact"
        ,"pending phone work reports every issued snapshot employee and assignment epoch separately; legacy aggregate-only reports remain explicitly unbound"
        ,"offline work begun while snapshot, credential, and assignment authority were valid can activate after reconnect, while work begun after revocation, expiry, or reassignment is rejected"
        ,"native wire timestamps are canonical UTC milliseconds; immutable activation boundaries and catalog-derived authority restoration remain healthy after negative mutations"
        ,"complete Weekly Schedule day-change batches pass the database receipt-chain health gate; exact replay is read-only and prefix, extension, reorder, and content conflicts fail atomically"
        ,"the retired 09:45 background writer has no scheduled runtime; only an explicit manager-approved schedule or absence publication may rebalance current static-weekly coverage"
        ,"every active employee-device credential is confirmed and matches the deployed DEVICE_CREDENTIAL_SECRET generation before canary resume; an unconfirmed or mismatched credential requires manager-code recovery that preserves phone-local work and never restores a legacy secret fallback"
      ]
    }
  },
  "rollback": {
    "before_enforcement": "legacy Engine and current backend completion remain available",
    "after_enforcement": "durably pause the exact physical canary and use the reviewed forward restoration set; never re-enable a legacy or direct-SQL writer",
    "database": "forward-only; do not down-migrate contexts, proofs, reservations, reconciliation, audit, scan evidence, dispositions, or outbox evidence"
  }
}
